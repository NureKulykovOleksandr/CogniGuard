version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    environment:
      - PORT=3000
      - MONGO_URI=mongodb://fkoolikov_db_user:HbzGqFXwgUER0Fft@ac-oduj4ys-shard-00-00.kl9rx8b.mongodb.net:27017,ac-oduj4ys-shard-00-01.kl9rx8b.mongodb.net:27017,ac-oduj4ys-shard-00-02.kl9rx8b.mongodb.net:27017/CogniGuardDB?ssl=true&replicaSet=atlas-wp0u7b-shard-0&authSource=admin&appName=Cluster0
    expose:
      - "3000"

  nginx:
    image: nginx:alpine
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "3000:80"
    depends_on:
      - backend
